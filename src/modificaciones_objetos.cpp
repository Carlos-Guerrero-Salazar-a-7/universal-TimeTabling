#include "../include/modificaciones_objetos.h"
#include <cstring>
#include <cstdio>

bool Modificador::obtenerValor(const char* instruccion, const char* clave, char* valor, int max_len) {
    return false;
}

void Modificador::parsearCadenas(const char* input, char** output, int& cantidad, int max_items, int max_len) {
    cantidad = 0;
}
